import { React } from 'react';

const Footer = ({ name }) => {
	return <footer className='bg-gray-200 text-center text-xs p-3 absolute bottom-0 w-full'>Hello {name}</footer>;
};

export default Footer;
